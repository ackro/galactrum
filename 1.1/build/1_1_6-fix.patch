--- src/validation.cpp.old
+++ src/validation.cpp
@@ -1247,7 +1247,7 @@
     } else if (nHeight < 100000) {
         return blockValue * 0.6;
     } else if (nHeight < 110000) {
-        return blockValue * 0.7
+        return blockValue * 0.7;
     }
     return blockValue * 0.8;
 }
